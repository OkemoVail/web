import { chromium } from 'playwright'; import path from 'path'; (async () => { const browser = await chromium.launch(); const page = await browser.newPage(); page.on('console', msg => console.log('PAGE LOG:', msg.text())); page.on('pageerror', error => console.error('PAGE ERROR:', error)); await page.goto('file://' + path.resolve('word/index.html')); try { await page.click('#tb-bold'); console.log('Clicked tb-bold'); } catch(e) { console.log('Error clicking:', e); } await browser.close(); })();